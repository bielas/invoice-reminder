First run at the root of application -> it will fetch all dependencies:

    npm install
   
...and run application by:

    npm run dev

Now it's running! Go to `http://localhost:5000/` and explore the API!
